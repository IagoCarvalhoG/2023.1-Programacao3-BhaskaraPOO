public class Cinrcunference {
    private double radius;
    public Cinrcunference(){

    }
    private double radius(double x1, double y1, double x2, double y2){
        double radius = Math.sqrt(Math.pow((BhaskaraFinder.getX2-BhaskaraFinder.getX1),2) + (Math.pow((BhaskaraFinder.getY2-BhaskaraFinder.getY1));
    }
}
